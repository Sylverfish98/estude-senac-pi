[project]
name = "estude"
version = "0.1.0"
description = "Um simples e eficiente aplicativo para a genrencia de estudos"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.2",
    "gunicorn",
]
